# Non-JAX dependencies for learn_lpns.
#
# Install JAX for your platform first (matching jax + jaxlib), then run:
#   pip install -r requirements.txt
#
# CPU:
#   pip install -U "jax[cpu]"
# CUDA (pick the variant that matches your toolkit — see https://docs.jax.dev/en/latest/installation.html ):
#   pip install -U "jax[cuda12]"

numpy>=1.24,<3
scipy>=1.10
matplotlib>=3.7
pandas>=2.0
vtk>=9.2
optax>=0.2
dill>=0.3.6
tqdm>=4.65
